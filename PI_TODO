
Mini-problemas a resolver se der:
    Suportar a existência de palavras com dois significados, por exemplo, swicth em inglês, tanto pode ser "trocar" como "interruptor" em português

A fazer:

Lidar com noticias(para já tirei só)

fazer edit news ser uma textArea

melhorar informação do popUps





Edit Button --import -- Funcional -- falta melhorar design

Filtros e ordenação na tabela

Fazer POST/GET para noticias --- medio import 

Permissões gerais e também, por exemplo, só poder adicionar palavras se tiver mais permissões -- menos import
Meter bandeirinhas a corresponder à lingua -- menos import

aplicação de extrair strings -- import

mostrar palavras que nao têm tradução

default lingua pc




Erros a corrigir:

popup nao deixar carregar noutro sitio


Error handling a implementar:

verificar se a falha no fetch da sempre


Documentar:
	-- script run
	-- erro de fetch
	-- Change Words
	-- o porque de NoSql
	-- o porque da estrutura da DB
	-- a API ??
	
	-- Tudo o resto !!!
	
    *para melhorar, ver o $t, passar o rato por cima e ver documentação


Backend:
	Express js, Node js, Mongo DB -- DONE
	Site simples exemplo



DONE: ----Popular todos os editáveis desde o iicio -> Perguntar se há algum problema, e se era isto que prejudicava a performance   
    
    
DONE: ----- Resolver erros tabela, quando se muda de lingua a seguir a mudar palavra
e quando se muda palavra de uma lingua que não é a que se está

fiz assim -- update ao current messages quando o locale é o mesmo, update idioms quando o locale nao é o mesmo, watch quando muda a locale, update current messages para idioms[locale]
-----agora só não muda depois de mudar de locale, talvez porque novo current messages nao é um proxy(era isto)

DONE:--- Pagina com tabela de todo o vocabulario

DONE --- erro de fetch

DONE --- fazer script para inicializar site

DONE --- Ter noticias para cada idioma OU arranjar forma de traduzir as noticias 

DONE --- Corrigir erro troca de strign
dar fetch a um idioma de cada vez--(meio que ja esta, falta melhorar)fazer depois quando tiver a minha API

DONE --- Se nao conseguir fazer com que nao de refresh, nao fazer nada em js, simplesmente dar reload --
-- não é preciso js, nem refresh, porque eu estou a mudar o conteudo das current messages -- DONE


